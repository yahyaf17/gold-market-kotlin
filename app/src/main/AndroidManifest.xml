<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mandiri.goldmarket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_gold_market"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_gold_market_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoldMarket">
        <activity android:name=".onboarding.OnboardingActivity"></activity>
        <activity android:name=".maintab.MainTabActivity" />
        <activity android:name=".onboarding.RegisterActivity" />
        <activity android:name=".onboarding.LoginActivity" />
        <activity android:name=".splash.WelcomeActivity" />
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>